# Neon Postgres (use a URL pooled com sslmode=require).
DATABASE_URL=postgresql://USER:PASSWORD@HOST-pooler/DBNAME?sslmode=require

# Segredos distintos e longos para access token e refresh token.
JWT_ACCESS_SECRET=replace-with-a-long-random-string
JWT_REFRESH_SECRET=replace-with-another-long-random-string

# Endereços usados no desenvolvimento local.
PORT=3000
CORS_ORIGIN=http://localhost:5173

# Opcional em produção: URL direta para aplicar o schema.
# DATABASE_URL_UNPOOLED=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
